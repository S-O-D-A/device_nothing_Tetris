<resources>
    <!-- Whether to show min refresh rate in display settings -->
    <bool name="config_show_min_refresh_rate_switch">true</bool>
 
    <!-- Show battery cycle count -->
    <bool name="config_show_battery_cycle_count" translatable="false">true</bool>

    <!-- The radius of the enrollment progress bar, in dp -->
    <integer name="config_udfpsEnrollProgressBar" translatable="false">100</integer>

    <!-- Whether to show peak refresh rate in display settings -->
    <bool name="config_show_peak_refresh_rate_switch">true</bool>

    <!-- Default for Settings.Global.DISABLE_WINDOW_BLURS -->
    <integer name="def_disable_window_blurs">0</integer>

    <!-- Shows the Lift to wake toggle option inside Settings UI -->
    <bool name="config_show_lift_to_wake">true</bool>

    <!-- Pocket Mode menu option ko settings me enable karne ke liye -->
    <bool name="config_show_pocket_mode">true</bool>
    
</resources>
